apiVersion: apps/v1
kind: Deployment
metadata:
  name: text-worker
  namespace: kcc
  labels:
    app: text-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: text-worker
  template:
    metadata:
      labels:
        app: text-worker
    spec:
      containers:
        - name: text-worker
          image: kissanacr.azurecr.io/agent-worker:latest
          env:
            - name: KAFKA_TOPIC
              value: "farmer-messages-text"
          envFrom:
            - configMapRef:
                name: kcc-config
            - secretRef:
                name: kcc-secrets
          resources:
            requests:
              cpu: "200m"
              memory: "1Gi"
            limits:
              cpu: "500m"
              memory: "2Gi"
          readinessProbe:
            exec:
              command: ["python", "-c", "print('ready')"]
            initialDelaySeconds: 60
            timeoutSeconds: 5
            periodSeconds: 15
          livenessProbe:
            exec:
              command: ["python", "-c", "print('alive')"]
            initialDelaySeconds: 90
            timeoutSeconds: 5
            periodSeconds: 30
